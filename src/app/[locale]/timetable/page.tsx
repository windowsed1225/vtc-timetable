import TimetableRoute from "@/components/TimetableRoute";

// Named timetable route: the full calendar workspace, opening on the month grid.
export default function Page() {
	return <TimetableRoute mode="timetable" />;
}
