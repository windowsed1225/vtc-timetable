import AttendancePage from "@/components/AttendancePage";

// Full attendance route — replaces the attendance panel that used to live in the sidebar.
export default function Page() {
	return <AttendancePage />;
}
